# This Yaml Document has been converted by ESAI Yaml Pipeline Conversion Tool.
# Please make sure to check all the converted content, it is your team's responsibility to make sure that the pipeline is still valid and functions as expected.
# This pipeline will be extended to the Custom template
# The pool section has been filled with placeholder values, check the following link for guidance: https://eng.ms/docs/cloud-ai-platform/devdiv/one-engineering-system-1es/1es-docs/1es-pipeline-templates/onboardingesteams/overview, replace the pool section with your hosted pool, os, and image name. If you are using a Linux image, you must specify an additional windows image for SDL: https://eng.ms/docs/cloud-ai-platform/devdiv/one-engineering-system-1es/1es-docs/1es-pipeline-templates/features/sdlanalysis/overview#how-to-specify-a-windows-pool-for-the-sdl-source-analysis-stage
# The 'DownloadPipelineArtifact@2' tasks have been converted to inputs within the `templateContext` section of each job.
# The Task 'NuGetCommand@2' has been converted to a wrapper task named 'NuGet push'.
# The Task 'NuGetCommand@2' has been converted to a wrapper task named 'NuGet push'.
trigger: none
name: $(Date:yyyyMMdd).$(Rev:r)
resources:
  pipelines:
  - pipeline: '_microsoftnode-api-dotnet'
    project: 'ISS'
    source: 'microsoft.node-api-dotnet'
    trigger:
      branches:
        include:
        - main
  repositories:
  - repository: CustomPipelineTemplates
    type: git
    name: 1ESPipelineTemplates/OfficePipelineTemplates
    ref: refs/tags/release
extends:
  template: v1/Office.Official.PipelineTemplate.yml@CustomPipelineTemplates
  parameters:
    pool:
      name: 1ESPT-Pool-Name-Placeholder
      image: pool-image-placeholder
      os: windows
    customBuildTags:
    - ES365AIMigrationTooling-BulkMigrated-Release
    stages:
    - stage: Stage_1
      displayName: Nuget ms/react-native feed
      jobs:
      - job: Job_1
        displayName: Publish Nuget
        condition: succeeded()
        timeoutInMinutes: 0
        templateContext:
          inputs:
          - input: pipelineArtifact
            pipeline: '_microsoftnode-api-dotnet'
            artifactName: 'published-packages'
            targetPath: '$(Pipeline.Workspace)/published-packages'
        steps:
        - task: 1ES.PublishNuGet@1
          displayName: 'NuGet push'
          inputs:
            packageParentPath: '$(Build.ArtifactStagingDirectory)'
            packagesToPush: $(System.DefaultWorkingDirectory)/_microsoft.node-api-dotnet/published-packages/Microsoft.JavaScript.NodeApi.0.*.nupkg;$(System.DefaultWorkingDirectory)/_microsoft.node-api-dotnet/published-packages/Microsoft.JavaScript.NodeApi.Generator.0.*.nupkg;$(System.DefaultWorkingDirectory)/_microsoft.node-api-dotnet/published-packages/Microsoft.JavaScript.NodeApi.DotNetHost.0.*.nupkg
            nuGetFeedType: external
            externalEndpoint: Nuget - ms/react-native-public
    - stage: Stage_2
      displayName: npm ms/react-native feed
      jobs:
      - job: Job_1
        displayName: Agent job
        condition: succeeded()
        timeoutInMinutes: 0
        templateContext:
          inputs:
          - input: pipelineArtifact
            pipeline: '_microsoftnode-api-dotnet'
            artifactName: 'published-packages'
            targetPath: '$(Pipeline.Workspace)/published-packages'
        steps:
        - task: NodeTool@0
          displayName: Use Node 18.x
          inputs:
            versionSpec: 18.x
        - task: CmdLine@2
          displayName: Setup npmrc file for react-native feed
          inputs:
            script: |
              echo registry=https://pkgs.dev.azure.com/ms/_packaging/react-native/npm/registry/ > _microsoft.node-api-dotnet\published-packages\.npmrc
              echo always-auth=true >> _microsoft.node-api-dotnet\published-packages\.npmrc
        - task: npmAuthenticate@0
          displayName: npm Authenticate .npmrc
          inputs:
            workingFile: _microsoft.node-api-dotnet\published-packages\.npmrc
            customEndpoint: Npm - ms/react-native
        - task: CmdLine@2
          displayName: npm publish to react-native feed
          inputs:
            script: |-
              cd _microsoft.node-api-dotnet\published-packages
              for %%i in (*.tgz) do npm publish %%i
    - stage: Stage_3
      displayName: npm npmjs.com feed
      jobs:
      - job: Job_1
        displayName: Agent job
        condition: succeeded()
        timeoutInMinutes: 0
        templateContext:
          inputs:
          - input: pipelineArtifact
            pipeline: '_microsoftnode-api-dotnet'
            artifactName: 'published-packages'
            targetPath: '$(Pipeline.Workspace)/published-packages'
        steps:
        - task: NodeTool@0
          displayName: Use Node 18.x
          inputs:
            versionSpec: 18.x
        - task: CmdLine@2
          displayName: Setup npmrc file for npmjs.com feed
          inputs:
            script: |
              echo registry=https://registry.npmjs.org > _microsoft.node-api-dotnet\published-packages\.npmrc
              echo always-auth=true >> _microsoft.node-api-dotnet\published-packages\.npmrc
        - task: npmAuthenticate@0
          displayName: npm Authenticate .npmrc
          inputs:
            workingFile: _microsoft.node-api-dotnet\published-packages\.npmrc
            customEndpoint: npm - rnbot - registry.npmjs.org
        - task: CmdLine@2
          displayName: npm publish to npmjs.com feed
          inputs:
            script: |-
              cd _microsoft.node-api-dotnet\published-packages
              for %%i in (*.tgz) do npm publish %%i
    - stage: Stage_4
      displayName: Nuget nuget.org feed
      jobs:
      - job: Job_1
        displayName: Publish Nuget
        condition: succeeded()
        timeoutInMinutes: 0
        templateContext:
          inputs:
          - input: pipelineArtifact
            pipeline: '_microsoftnode-api-dotnet'
            artifactName: 'published-packages'
            targetPath: '$(Pipeline.Workspace)/published-packages'
        steps:
        - task: 1ES.PublishNuGet@1
          displayName: 'NuGet push'
          inputs:
            packageParentPath: '$(Build.ArtifactStagingDirectory)'
            packagesToPush: $(System.DefaultWorkingDirectory)/_microsoft.node-api-dotnet/published-packages/Microsoft.JavaScript.NodeApi.0.*.nupkg;$(System.DefaultWorkingDirectory)/_microsoft.node-api-dotnet/published-packages/Microsoft.JavaScript.NodeApi.Generator.0.*.nupkg;$(System.DefaultWorkingDirectory)/_microsoft.node-api-dotnet/published-packages/Microsoft.JavaScript.NodeApi.DotNetHost.0.*.nupkg
            nuGetFeedType: external
            externalEndpoint: nuget.org/cxe/microsoft.javascript